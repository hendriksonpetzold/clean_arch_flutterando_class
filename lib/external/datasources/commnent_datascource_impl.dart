import 'package:clean_arch/infra/datasources/comment_datasources.dart';

class CommentDatascourcesImpl implements CommentDatasources {
  @override
  Future<List> getComments() {
    //TODO IMPLEMENTS HTTP
    throw UnimplementedError();
  }
}
