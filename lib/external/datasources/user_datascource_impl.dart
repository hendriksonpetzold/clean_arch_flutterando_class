import 'package:clean_arch/infra/datasources/user_datasources.dart';

class UserDatascourcesImpl implements UserDatasources {
  @override
  Future<List> getUsers() {
    //TODO IMPLEMENTS HTTP
    throw UnimplementedError();
  }
}
